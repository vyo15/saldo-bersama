- Lifecycle overlay sekarang memakai history coordinator canonical di `Modal`: perpindahan langsung antar modal melakukan handoff marker tanpa race `history.back()`/`pushState()`, sehingga dialog dapat ditutup lalu dibuka ulang realtime tanpa refresh manual. Body `modal-open` memakai reference count dan focus lama hanya dipulihkan bila tidak ada dialog aktif lain; feature tidak memiliki lifecycle `popstate` sendiri.
- Microcopy educational sekarang mengikuti hierarchy satu-fakta-satu-surface: modal/list helper tidak mengulang penjelasan yang sama, mobile Rekening memakai satu Info canonical, dan detail yang memengaruhi keputusan finansial tetap persistent. Helper presentasi transaksi yang dieksekusi langsung oleh Node test dipisahkan dari registry ikon React agar frontend regression tidak mencoba memuat `.jsx` melalui Node ESM.
- **Investasi asset-centric saham + Reksa Dana:** route `/investasi` menampilkan aset langsung tanpa hierarchy broker/Ajaib/Bibit/Indodax atau card RDN. `Tambah investasi` membuka saham/reksa dana, lalu posisi disimpan lewat `investments.assets.create`; katalog saham LQ45 dan reksa dana user tetap presentation scope prototype. Tidak ada koneksi broker, marketplace, NAV live, auto-sync, atau order execution.
- Sistem ikon finansial sekarang memakai taxonomy semantik canonical: rekening generik/tipe rekening, saldo netral tanpa simbol dolar, arus masuk/keluar/transfer, kategori literal, serta trend dinamis. Mapping tipe rekening dipusatkan pada `financeChoiceIconRegistry.js`; `FiCreditCard` dibatasi ke metode pembayaran dan `FiDollarSign` tidak lagi dipakai di source UI. Dashboard, Transaksi, Rekening, Investasi, Kategori, Laporan, Target, Rekonsiliasi, Anggaran, dan Pengaturan telah diselaraskan tanpa mengubah business logic.
- **Flow Investasi sederhana:** tab user-facing adalah **Aset / Aktivitas** dengan filter Semua/Saham/Reksa Dana. Detail aset membawa kepemilikan, modal, harga terakhir, nilai, P/L, dan aksi Beli/Jual/Perbarui nilai. Broker/portfolio tetap ada hanya sebagai compatibility backend untuk histori v15/v16 dan tidak diekspos sebagai mental model produk.
- Rekening Investasi legacy tetap memiliki visual canonical pada menu Rekening bila memang merupakan rekening user existing. Compatibility account internal yang dibuat flow asset-centric ditandai `is_system_hidden=1` dan tidak muncul di Rekening/picker user-facing.
- Rekening mobile dipoles ulang: header sekarang `Rekening` + contextual info + `Tambah`, CTA `Transfer` tunggal berada di quick actions `Riwayat / Transfer`; tap kartu aktif menjadi entry point detail/kelola, picker lama dipensiunkan, dan renderer meng-clip ShopeePay/E-wallet agar sudut rounded menyatu tanpa fringe gelap.
# Project Status

## Native-feel application continuity — 9–10 September 2026

- Feature-page initial load sekarang memakai skeleton per-domain yang mengikuti geometri Dashboard, Rekening, Transaksi, Planning, Target, Investasi, Laporan, Rekonsiliasi, serta list/settings; spinner/full-screen loader dibatasi ke verifikasi session/auth dan blocking state. Route cepat tetap anti-flicker, sedangkan route yang benar-benar menunggu chunk/data dapat menunjukkan progress tipis tanpa mengganti shell/sidebar/navigation.
- Lazy action user-facing memakai modal/panel shell yang langsung terlihat dan action prefetch berbasis intent/idle. Quick-add transaksi dapat diprefetch setelah app idle bila koneksi tidak Save-Data/2G; contextual prefetch tetap dibatasi pada chunk yang tidak membuat dependency cycle.
- AppShell mempertahankan data lama saat refresh, melakukan refresh silent setelah reconnect dan resume dari background yang stale, menampilkan offline/degraded/recovering sebagai status compact, dan memakai `visualViewport` untuk keyboard inset mobile. Notification unread count dapat diteruskan ke App Badge API bila platform mendukung.
- Installed PWA shortcut **Tambah transaksi** membuka `/transaksi?compose=1`, lalu composer dibuka dan query dibersihkan dengan replace. Update service worker v11 tidak dapat melakukan restart saat modal/composer/mutation aktif. Cold offline/session verification gagal aman melalui branded session gate tanpa menyimpan credential atau financial snapshot baru di storage.
- Modal canonical memasang history entry sementara sehingga Back menutup overlay sebelum route; state non-dismissible/mutation tetap fail-closed terhadap Back. PWA manifest tidak lagi mengiklankan `window-controls-overlay` sebelum titlebar custom benar-benar tersedia.
- Form finansial utama yang dapat kehilangan input memakai dirty-state guard app-owned. **Batal** sekarang langsung membuang draft lokal dan menutup form tanpa konfirmasi kedua supaya user dapat membuka edit/create lagi dengan state fresh; Back/X/backdrop/Escape dan `beforeunload` tetap melindungi draft yang berubah dari dismiss tidak sengaja. Mutation pending/`OUTCOME_UNKNOWN` tetap non-dismissible; mutation authority, idempotency, row-version, serta validation backend tidak berubah. Progress canonical bergerak melalui fill native `<progress>`, nominal berubah langsung tanpa count-up tetapi mendapat soft update cue, dan list utama memakai enter motion ringan yang otomatis mati pada reduced-motion.

## Financial flow regression hardening — 9 September 2026

- Pembelian Investasi menampilkan weighted average sebelum/sesudah pembelian otomatis dari holding canonical; user tetap memasukkan lot/unit dan harga per lembar/unit. Buy/Sell v17 adalah catatan posisi accounting-only (`cash_effect_enabled=0`) dan tidak memutasikan RDN; rekonsiliasi portfolio lama tetap tersedia untuk compatibility tetapi tidak menjadi flow utama asset-centric.
- Notification Center memakai read-state lokal yang tersinkron dalam tab yang sama. Alert funding-gap Alokasi sekarang benar-benar berasal dari backend ketika total Kebutuhan > dana yang dialokasikan, lalu deep-link membuka Alokasi yang tepat dengan nominal gap.
- Cocokkan Saldo non-Investasi memakai reconciliation terakhir sebagai checkpoint. Mismatch tetap historis dan tidak terus menghasilkan alert difference; reminder berikutnya hanya stale cadence setelah checkpoint. Attention route Dashboard/Notification Center dikonsumsi tanpa stale ref.
- Alokasi Dana mempertahankan invariant ledger: fund/release tidak mengubah saldo bank fisik, tetapi mengubah `available_balance`. Aktivitas fund/release sekarang terlihat bersama realokasi; desktop Rekening memprioritaskan Dana tersedia agar sisa uang bebas tidak tersamarkan.
- Mobile tidak lagi merender topbar brand/theme global; theme tetap dapat diubah dari satu kontrol berlabel di `Menu lainnya`. Account carousel menangani cancelled horizontal gesture tanpa forced snap-back dan root memakai horizontal overscroll containment. Laporan tidak lagi merender alert operasional; kategori memakai semantic tone expense/income/refund yang berbeda.


## Accessibility dan theme hardening

- Semantic contrast sekarang diuji dua tema dengan alpha compositing: status `*-soft`, selected `primary-soft`, hero muted text, hero focus, notification badge, danger hover, wordmark/dock active, dan recurring counter telah ditutup ke threshold WCAG yang relevan. `theme-color` runtime membaca `--page`, fallback PWA mengikuti light `--page`, dan build-budget threshold canonical ikut dikunci regression.

## Mobile app experience hardening

- Temporal input app-owned sekarang canonical lintas feature: 27 field date/month/time memakai `TemporalInput`/`TemporalPickerField`; picker dari dalam modal memakai same-modal subview, DatePicker/MonthPicker/TimePicker mempertahankan string lokal `YYYY-MM-DD`/`YYYY-MM`/`HH:mm`, dan tidak ada native date/month/time picker di `frontend/src`.
- Picker rekening transaksi sekarang logo-first: BCA/BNI/BTN/Mandiri/Permata dan ShopeePay/DANA/GoPay/OVO/LinkAja memakai logo compact transparan terpisah dari artwork kartu; rekening non-brand tetap memakai semantic icon. Hierarchy mobile dipisahkan menjadi nama rekening, kepemilikan, dan nominal; selected state memakai primary-soft + border + circular check.
- True-empty surface planning sekarang memakai satu sumber aksi: Alokasi Dana, Kebutuhan detail, Jadwal Rutin, Target, dan setup Investasi tidak menampilkan summary/hero/toolbar nol sekaligus dengan empty-state CTA. Filtered-empty mengarahkan reset/tampilkan data yang tersedia, bukan create baru.
- Route Transaksi mobile tidak menampilkan CTA `Tambah transaksi` kedua di PageHeader atau true-empty state; quick-add `+` pada bottom navigation tetap menjadi entry point composer canonical. Desktop tetap memiliki CTA lokal karena tidak memiliki quick-add mobile.
- Mobile `<=820px` sekarang memakai document scroll tanpa visual browser scrollbar, sementara root horizontal clipping tetap dilarang agar overflow bug tidak disamarkan. Intentional horizontal scroller (report harian, approval tabs, account carousel) tetap swipeable tetapi tanpa batang scrollbar.
- Hover shared/mobile dibatasi ke fine pointer dan touch mendapat pressed state. Bottom navigation, safe-area, 100dvh, scroll restoration, native 16px form control, focus-visible, dan reduced-motion contract tetap dipertahankan.
- Perpindahan route authenticated sekarang memakai content-enter motion ringan (`--motion-fast`) sementara shell/sidebar/header tetap diam. Internal link same-origin melakukan intent-prefetch pada hover/focus/pointer-down, Suspense content memakai delayed loader anti-flicker, dan initial app/login loader tetap immediate. Button/IconButton/FAB/bottom-nav memakai tactile press yang konsisten tanpa mengubah business rule.
- PWA install prompt hanya dipresentasikan pada Dashboard setelah engagement ringan (kunjungan berulang, sesi aktif, atau beberapa interaksi), memiliki `Nanti` dengan cooldown lokal tujuh hari, dan tidak menjadi authority/runtime dependency. Offline/degraded/recovering, safe update, dan warm-up route shell penting mengikuti lifecycle app tanpa mempersist financial snapshot atau credential baru.
- Planning mobile tidak mengulang heading child di dalam tab, Pusat Persetujuan dipadatkan menjadi grouped action surface, Jadwal Rutin/Alokasi/Settings/Investasi/Transaksi/Rekening menerima touch/hover polish, dan Rekonsiliasi memprioritaskan workflow utama dengan riwayat mobile sebagai disclosure sekunder. Audit Activity sengaja tidak diubah pada hardening ini.
- Dashboard Rekening desktop tetap memakai `AccountVisual` langsung tanpa nested surface per rekening: section `Rekening` menjadi container utama, artwork membawa shadow canonical, sedangkan Saldo/Dana tersedia dan metadata berada langsung di bawah kartu. Beranda mobile sengaja tidak lagi merender kartu ATM/rekening; akses cepat `Rekening` membuka route `/rekening`, sehingga summary dashboard tetap compact dan detail account carousel memiliki satu home canonical.
- Canvas Beranda desktop sekarang memakai max-width terkontrol dan hierarchy yang lebih tegas tanpa menyentuh curved sidebar: **Saldo rekening + Perlu dilakukan** menjadi baris utama, carousel Rekening dipertahankan sebagai konteks transaksi, kemudian **Transaksi terbaru** dipasangkan dengan stack **Investasi + Pengeluaran**, dan Kebutuhan/Jadwal Rutin/Target diringkas dalam satu section **Perencanaan keuangan**. Planning yang masih kosong memakai empty-action dashed yang jelas sebagai aksi, bukan record finansial; workflow create canonical dibuka langsung bila capability/prerequisite cukup.

Dokumen ini adalah snapshot kondisi project sekarang, bukan jurnal perubahan.

## UX hardening first-run dan bantuan kontekstual

- Dashboard first-run menempatkan checklist setup incomplete sebelum quick actions pada mobile dan membuka otomatis disclosure hanya saat progres masih `0/4`.
- Istilah Rekening `Saldo rekening`, `Dana tersedia`, dan `Dialokasikan` memakai copy canonical yang sama dengan glossary; helper tampil dekat nominal pada detail Rekening dan ringkasan Dashboard agar Alokasi Dana tidak dibaca sebagai uang tambahan.
- Pengaturan sekarang responsif secara arsitektur: mobile tetap memakai grouped-list ringkas Umum/Data/Sistem, sedangkan desktop memakai workspace tiga panel **Kategori → Menu → Detail** agar layar lebar tidak terasa seperti mobile yang dibentangkan. Kategori desktop memisahkan Umum, Data, Sistem, Integrasi, serta Sesi & keamanan; metadata route dipusatkan di `settingsNavigation.js`. Export/Import/Backup/Pemulihan tetap berada di hub `/pengaturan/data`, sedangkan Reset Testing/Reset Semua tetap terisolasi sebagai dua tab lazy di `/pengaturan/pemeliharaan`. Preference boolean notifikasi memakai switch semantic; acknowledgement destructive tetap checkbox. Route reset lama hanya redirect kompatibilitas.
- Landing Pengaturan mobile memakai **profile card canonical**: `UserAvatar` menampilkan foto profil Google bila tersedia dan fallback inisial bila tidak, identitas/email/role/zona waktu mempunyai hierarchy tersendiri, dan status backend berada pada baris status terpisah. Card tidak membuat sumber profil baru; data tetap berasal dari session/auth yang sama.

## Runtime canonical

- **Frontend:** React 19 + Vite 7 PWA.
- **Backend:** Vercel Functions.
- **Database/source of truth:** Turso/SQLite HTTP pipeline.
- **Auth desktop dan mobile:** desktop memakai floating auth card dengan satu scene pasangan yang lebih proporsional dan tanpa headline tengah, sementara mobile mempertahankan onboarding/login empat halaman; tombol Google branded Saldo Bersama memakai contract UI yang sama, tetapi transport production dan development dipisah pada loading boundary. Pada production canonical `saldo-bersama.vercel.app`, tombol siap melalui module server-OAuth ringan tanpa mengunduh Firebase browser Auth, lalu browser memulai Google OAuth Authorization Code flow ke `/api/auth/google/start`; callback server memverifikasi signed `state`/`nonce` + PKCE S256, menukar code menjadi Google ID token, menukarnya lagi melalui Firebase Identity Toolkit menjadi Firebase ID token, lalu memakai verifier Firebase + registry `users` canonical sebelum membuat registered HttpOnly server session. Localhost/device emulation lazy-load Firebase popup sebagai fallback development. Mobile menyimpan hanya flag presentasional bahwa onboarding pernah dilihat agar returning device langsung ke login; flag tidak menjadi authority dan onboarding dapat dibuka ulang. Onboarding mobile memakai swipe, pagination klik, keyboard ArrowLeft/ArrowRight, dan `Lewati` tanpa CTA `Kembali`/`Lanjut`; halaman login akhir tetap mempertahankan `Ulang`. Tidak ada Firebase browser redirect state pada production.
- **Session/authorization authority:** signed HttpOnly session v2 membawa credential opaque; `user_sessions` + status/role/binding Firebase UID pada `users` adalah authority runtime. `ALLOWED_USERS_JSON` hanya bootstrap/recovery Administrator pertama. User dapat melihat/revoke session perangkat miliknya; role change/deactivation mencabut session aktif.
- **Google integration:** Apps Script bridge; Sheets mirror satu arah, Calendar reminder bersama, Drive backup teknis.
- **Abuse/operational health:** process-local limiter tetap lapisan murah, sedangkan bucket Turso v13 menjadi counter lintas instance untuk gateway/export/login/OAuth. Public `/api/health` tetap minim data dan merepresentasikan **core availability**: database/schema/binding, maintenance, serta integrity failure. Scheduler, integration dead-letter, notification/dead-letter, dan backup failure tetap muncul pada `system.health` terautentikasi sebagai operational warning dan tidak mematikan login/ledger ketika core sehat; partial Push delivery tetap mendegradasi scheduler heartbeat. External alert delivery tetap belum dipilih/diimplementasikan.
- **Active schema contract:** v17.
- **Kolaborasi Member v14 (dipertahankan pada runtime v17):** Member dapat mengajukan rekening/kategori baru dan transfer shared → personal melalui request backend; Administrator mereview dengan `row_version` + idempotency, dan approval transfer membuat tepat satu ledger canonical secara atomik. Transfer personal lintas pemilik diizinkan dengan ownership ledger mengikuti rekening sumber/debit.
- **Investasi manual v17 asset-centric:** aset investasi di aplikasi lain hanya **dicatat**, bukan dihubungkan sebagai broker/trading API. Route `/investasi` langsung menampilkan saham/reksa dana, hero **Total investasi tercatat** memakai `summary.market_value`, tab **Aset / Aktivitas**, filter jenis aset, dan detail per aset; broker/source/RDN/portfolio tidak lagi menjadi hierarchy UI. Posisi pertama memakai `investments.assets.create`; backend membuat/reuse compatibility portfolio dan, bila perlu, rekening Rp0 tersembunyi `is_system_hidden=1`. Direct opening-position serta Buy/Sell v17 memakai `cash_effect_enabled=0`, sehingga pencatatan aset tidak mensyaratkan atau mengubah saldo RDN. `cash_amount` tetap disimpan untuk weighted cost basis/realized P/L. Perubahan market value normal dilakukan lewat **Perbarui nilai**: saham mengisi harga per lembar dan reksa dana mengisi NAB per unit, baik per aset maupun massal lewat `investments.valuations.bulkUpdate`; jumlah lot/unit tidak ikut berubah. Histori v15/v16 tetap mempertahankan cash effect melalui default `1` dan view `investment_account_events`, sehingga migration tidak menulis ulang ledger lama. Legacy portfolio/RDN/reconciliation/correction tetap tersedia sebagai contract maintenance/recovery dan data lama tetap readable, tetapi bukan workflow harian UI asset-centric. Dashboard desktop/mobile memakai `market_value` untuk ringkasan Investasi; compatibility `rdn_cash/portfolio_value` tidak menjadi total investasi user-facing. Authorization, `row_version`, idempotency, backup/restore, integrity guard, serta ownership tetap authoritative di backend.
- **Runtime DEV/PROD terisolasi:** `npm run dev` lebih dulu memverifikasi runtime Node 22.15.0+ (22.x) atau Node 24.x lalu hanya bootstrap/menulis `.env.local` Development dan selalu mencoba refresh Vercel Development; bila control-plane Vercel sementara tidak tersedia, cache `.env.local` yang sudah lengkap boleh dipakai tetapi Turso/schema/binding tetap harus lolos preflight fail-closed. `npm run prod` tidak menarik atau menimpa Development. Google bridge adalah grup pusat yang boleh diselaraskan DEV → PROD lokal hanya bila grup DEV lengkap dan PROD kosong; Turso/session/OAuth/VAPID tidak pernah disalin lintas environment. Aggregate health scheduler/integrasi boleh menjadi warning selama database/schema, anonymous `/api/session` (`401 UNAUTHENTICATED`), dan frontend core sehat; session `5xx`, integrity, maintenance, atau schema mismatch tetap blocker.
- Source v17 mempertahankan guard `DATABASE_ENVIRONMENT`/session v12 dan `rate_limit_buckets` durable untuk throttle lintas instance; binding database tetap fail-closed. Development dan Production sekarang merupakan dua runtime yang wajib terisolasi. ADR-0007 dipertahankan hanya sebagai catatan historis constraint satu-database; source/test aktif menolak database/token/session/VAPID yang dibagi lintas environment.

## Workflow saat ini

- Source terbaru + test aktual adalah sumber kebenaran.
- Tidak ada task card/Task ID/branch automation sebagai workflow wajib.
- Quality gate lokal canonical: `npm run verify`; `npm run zip` menjalankannya otomatis sebelum packaging dan pre-push guard menjalankannya lagi sebelum push. Rendered browser smoke memakai DevTools port ephemeral dari `DevToolsActivePort`, memantau browser exit, dan menguji `:focus-visible` lewat Tab CDP dengan fallback pseudo-state DevTools yang deterministik agar portable di Windows/GitHub Actions Linux. Handoff patch hanya berstatus final setelah gate tree final PASS pada runtime Node yang didukung; environment di luar rentang dukungan wajib menyebut artifact candidate/unverified. Setelah setiap verification PASS maupun gagal, generated build/test artifact dan cache Vite generated dibersihkan tanpa menghapus dependency, `.env.local`, `.vercel`, atau Git metadata.
- Frontend regression dependency boundary memverifikasi named import `FinanceChoiceIcons.jsx` terhadap export aktual dan menjaga `accountTypeIcon` tetap berasal dari `financeChoiceIconRegistry.js`, sehingga typo/mismatch export tidak baru diketahui setelah Vite membuka ErrorBoundary.
- Delivery rutin: commit pada `main` lalu `git push origin main`; pre-push memverifikasi ref/SHA aktual, clean working tree, fast-forward, dan full `npm run verify`. Diff schema/migration wajib melewati schema/binding Turso Production secara read-only; diff non-schema memakai core Vercel Production health tanpa credential database lokal. Runtime schema baru tidak boleh terdeploy sebelum Production DB kompatibel; push tidak pernah auto-migrate. Quality GitHub tetap berjalan server-side.
- `npm run zip` tetap menjalankan full verification dan sekarang clean-only: PASS membuat `saldo-bersama-clean.zip` secara atomic; verification gagal exit non-zero dan tidak membuat archive baru. Clean ZIP verified terakhir tidak ditimpa oleh hasil yang gagal. Artifact `saldo-bersama-UNVERIFIED.zip` hanya historical input remediation dari workflow lama, bukan output workflow saat ini.
- Guarded/high-risk tetap membutuhkan approval eksplisit sebelum coding/operation.

## UI foundation / maintainability saat ini

- Contract maintainability canonical sekarang berada di `docs/CODE_MAINTAINABILITY.md`: code menjelaskan WHAT, comment menjelaskan WHY/invariant, extraction mengikuti responsibility bukan line count, dan public facade service tetap stabil. Backend notifications, master data, reporting dashboard, recurring, goals, envelopes, serta pure read-model reset/full-reset sudah dipisah ke child module satu arah; governance menolak child yang mengimpor facade induk dan menolak circular dependency relatif backend.
- Guarded authority tetap terkonsentrasi: finance/saldo, auth/session, idempotency, destructive maintenance, dan hard DELETE allowlisted tidak dipindahkan menjadi presentation/helper authority. Frontend form/page besar memisahkan presentation sementara submit/idempotency/recovery/auth orchestration tetap pada parent canonical.
- Shared primitive canonical memakai CSS Modules. Global compatibility class tetap tersedia untuk direct `button`/`Link` legacy, tetapi state disabled dan control height sekarang mengikuti token/semantics yang sama dengan `Button.module.css`; duplicate override `min-height` sudah dihapus.
- Boundary responsive JavaScript memakai satu source of truth `frontend/src/config/layout.js` (`APP_BREAKPOINTS`/`APP_MEDIA`); feature tidak menyalin string `820/821px` untuk media-query runtime.
- `TransactionForm.jsx` tetap menjadi presentation/composition owner yang ringkas, sedangkan prefill, derived state, validation/focus-error, idempotency intent, save/finalization, dan continuation dikelola oleh `transactionFormController.js`; business invariant tetap canonical di domain/backend.
- Migrasi stylesheet feature transitional ADR-0009 sudah selesai untuk Dashboard, Login, Transactions, dan Notification Center. File global lama telah dihapus; feature layout/state sekarang berada pada CSS Module pemiliknya, sedangkan global CSS dibatasi ke reset/token/base/shared primitive/shell responsive.
- Token `--account-*` tetap global karena perlu theme-level contract, tetapi diberi ownership Accounts-only. `components/finance/` tetap target opsional dan belum dibuat sampai ada reusable consumer lintas feature nyata.
- Named import `react-icons/fi` kini memiliki regression contract yang memverifikasi symbol benar-benar diekspor package, sehingga typo/ikon yang tidak tersedia gagal pada test sebelum mencapai build.
- Route Transaksi dan workspace Perencanaan memakai lazy boundary untuk presentation/dialog berat. Perencanaan memiliki dua tab canonical: Alokasi Dana dan Jadwal Rutin; Kebutuhan dikelola langsung di detail Alokasi Dana. Surface Anggaran terpisah sudah dihapus agar tidak menduplikasi fungsi Kebutuhan; `/anggaran` dipertahankan hanya sebagai compatibility redirect ke `/perencanaan/kantong`. Build budget tetap memberi warning mulai 90% batas agar headroom rendah terlihat sebelum patch berikutnya menjadi failure.
- Source hygiene kini ikut digate: named export langsung harus memiliki consumer repository, asset visual runtime harus direferensikan, dan chunk JS non-route yang lazy dibatasi **32 KiB gzip** selain budget route 8 KiB. Route Alokasi memakai shell tipis `AllocationsPage` yang me-lazy-load `AllocationsWorkspace`, dan overlay/dialog kondisional dipisahkan lagi ke `AllocationOverlayLayer`, sementara workflow dynamic import dari Dashboard/Notification Center menangani kegagalan load dan fallback interaksi finansial tidak lagi kosong.
- Mobile `<=820px` memakai Manrope Variable dengan body 14px, body-sm 13px, sm 12.5px, xs 12px, serta bobot semantic semibold/bold 550/650. Native text/textarea yang memicu keyboard memakai token 16px terpisah untuk mencegah auto-zoom Safari; date/month/time memakai `TemporalInput` app-owned dan app-owned list memakai `SelectionField`, keduanya menjaga target sentuh efektif minimum 44×44px. Meaningful financial/status metadata dijaga sekitar 12px+, outer gutter/section/card normal memakai 16px dengan fallback 12/14px hanya pada `<=340px`, dan generic topbar serta route full-bleed memasukkan safe-area top sesuai ownership layout.
- Bottom navigation mobile mempertahankan geometri 72px/24px/12px dengan FAB 52px dan lima slot sama lebar; tab non-FAB memakai seluruh 72px sebagai hit area dan active state memiliki indikator bentuk selain warna. Primary tab Beranda/Transaksi/Laporan mempertahankan posisi scroll masing-masing, secondary route baru dimulai dari atas, dan browser Back/Forward memulihkan posisi entry history sebelumnya bila sudah direkam. Perubahan ini presentation-only dan tidak mengubah saldo, ledger, authorization, atau mutation contract.
- Feedback sukses finansial sekarang memakai primitive canonical `FinancialSuccessOverlay`: Pengeluaran, Pemasukan, Transfer, Refund, dan rekonsiliasi matched berbagi logo aplikasi + badge ceklis animasi, nominal utama/ringkasan yang tampil sejak frame awal, MoneyRain 10 note one-shot, safe-area mobile, focus trap, serta reduced-motion tanpa celebration. `transactions.create` dan `reconciliations.create` disupresi dari global mutation pill agar tidak menghasilkan feedback ganda; rekonsiliasi difference tetap memakai warning terpisah tanpa celebration.
- Flow `Cocokkan Saldo` tidak lagi menganggap system balance sebagai actual balance: attention/notifikasi hanya memilih rekening, lalu user menjawab `Ya, saldonya sama` atau `Tidak, berbeda`. Input saldo aktual baru muncul pada cabang berbeda, mismatch menampilkan preview selisih, dan mutation reconciliation tetap tidak membuat adjustment saldo otomatis.
- Motion system telah diseragamkan ke semantic token termasuk stagger. JavaScript scroll memakai helper reduced-motion-aware, durasi rAF stack membaca token CSS canonical, stack Rekening memakai swipe + Arrow Left/Right + pagination dots dan mematikan 3D travel pada reduced-motion, gesture stack hanya menginterpolasi transform/opacity, progress transition memakai scale transform, serta dekorasi Login/fatal-error berhenti sendiri alih-alih loop tanpa akhir.
- Notification Center in-app `/notifikasi` membaca `dashboard.overview.alerts` canonical dan dipresentasikan sebagai task inbox ringkas: semua tipe utama dinormalisasi menjadi aksi, entitas, satu fakta, dan chevron. Presentation mobile memakai satu gutter shell (tanpa double-padding), segmented filter compact, grouped surface dengan unread accent, back button non-card, dan `Baca semua` icon-only aksesibel. Dashboard/Notification Center memakai contextual attention yang sama, sehingga reconciliation rekening mengunci entity yang sudah diketahui dan kembali ke Notification Center setelah selesai. RDN/Investasi diarahkan ke reconciliation portfolio (Saldo RDN + holding), bukan generic saldo rekening; bila portfolio pada attention sudah tidak tersedia, UI memberi feedback informatif dan menampilkan data Investasi terbaru. Read/unread tetap UI state lokal ber-TTL dan tidak menyelesaikan kondisi finansial; `/pengaturan/notifikasi` tetap khusus **Notifikasi perangkat** (preferensi/Web Push).

## Transaksi mobile saat ini

- `/transaksi` pada viewport ≤820px memakai presentation history-first terpisah dari table/filter desktop. Page tidak mengulang heading body; periode menjadi anchor utama, diikuti grafik arus kas 6 bulan dari `reports.monthly`, metadata pemasukan/pengeluaran, lalu daftar ledger berkelompok tanggal.
- Filter cepat hanya mengekspos jenis umum, Search, dan Filter. Dialog lanjutan memakai type selector compact serta `SelectionField` untuk Alokasi Dana, rekening, kategori, dan pencatat; Pengembalian dan Penyesuaian tetap tersedia. Semua query tetap memakai `transactions.list` canonical dan pagination backend existing.
- Row memakai fallback judul informatif tanpa `Tanpa keterangan`, lalu metadata sekunder yang menghindari duplikasi dan dapat menampilkan merchant, rekening, kategori, serta nama pencatat lengkap. Badge tetap hanya untuk exception penting (managed recurring/goal, belum dialokasikan, cancelled). Detail transaksi memuat jenis, kategori, rekening, alokasi, pencatat, tanggal Asia/Jakarta, sumber, status, dan action capability existing.
- Mobile presentation memakai CSS Module `MobileTransactionHistory.module.css`; desktop table/filter dan detail modal memakai `TransactionsPage.module.css`. Backend finance, schema, saldo, authorization, audit, idempotency, serta login tidak berubah oleh redesign ini.

## Composer transaksi mobile saat ini

- Composer default mobile tetap memakai `TransactionForm` canonical, tetapi presentation field dipisah ke `MobileTransactionFields.jsx`. Jenis transaksi dan nominal tetap memakai domain/input canonical; tanggal tetap compact, sedangkan rekening, kategori, dan Alokasi Dana sekarang memakai entity picker inline yang konsisten dengan Buat Alokasi.
- Rekening, kategori, dan Alokasi Dana tidak membuka nested modal, subview selection, atau native select browser. Rekening sumber tetap memakai filter/ranking `sourceAccountPicker`, tujuan memakai compatibility canonical, Alokasi memakai `orderedEnvelopeOptions`, dan Kategori memakai grup `Sering dipakai` + `Semua kategori` saat taxonomy >6. Seluruhnya memakai `InlineSelectionPicker`, searchable ketika panjang, collapse setelah dipilih, dan hanya mengubah draft form tanpa mutation. File `MobileTransactionSelectionView.*` lama sudah tidak memiliki consumer dan dihapus.
- Metode pembayaran yang sedikit tetap berupa choice chips langsung (opsional; tap pilihan aktif lagi untuk mengosongkan nilai), Catatan auto-grow tanpa resize handle, dan `TransactionImpactPreview` memakai `Setelah transaksi` dengan hasil akhir + delta tanpa `Lihat dampak lengkap` yang mengulang informasi. Transfer mobile tetap memakai `MobileTransferFields`, dengan source/destination memakai picker rekening inline canonical dan preview `Setelah transfer` compact.
- Daftar entity yang panjang tetap bounded dan dapat di-scroll/swipe; pada mobile ≤820px indikator scrollbar visual `InlineSelectionPicker` dan `SelectionField` disembunyikan tanpa scroll lock. Tidak ada asset, schema, API, ledger, atau business mutation baru pada consistency sweep ini. Desktop composer tetap memakai field presentation existing dan domain/controller yang sama.
- Native `<select>` app-owned sudah dipensiunkan dari seluruh `frontend/src`. Single-select entity utama memakai `InlineSelectionPicker`/`InlineOwnershipPicker`: rekening pada Transaksi/Transfer/Alokasi/Bagi dana/Target/Jadwal Rutin/setup RDN/Rekonsiliasi, aset pada form Investasi, kategori utama pada Transaksi/Kebutuhan/Jadwal Rutin, serta target Alokasi pada pemindahan/funding/pembayaran aktual. Fixed options tetap memakai `VisualChoiceGroup`; enum, filter, template, dan advanced filter tetap memakai `SelectionField` agar density sesuai konteks. Input date/month/time native sudah dipensiunkan dari feature UI dan diarahkan ke `TemporalInput`; checkbox native tetap dipertahankan ketika semantik checklist tepat. Flow Buat Alokasi tetap `Nama alokasi → Ambil dana dari → Digunakan oleh → Periode dan sisa → Dana yang disiapkan`; business contract tidak berubah.
- Consistency sweep memperkuat `SelectionField` compact/embedded/search agar tetap 44px pada mobile, Notification Center dan Rekonsiliasi mengikuti hit-target yang sama, CTA navigasi memakai `ButtonLink` canonical, dan Reports desktop tidak lagi membawa branch/mobile CSS legacy yang tidak pernah dirender.

## Beranda mobile dan mutation safety saat ini

- Beranda mobile memakai data canonical `dashboard.overview` dengan hierarchy summary-first: hero **Saldo rekening** → satu `Perlu dilakukan` prioritas bila ada → setup → empat akses cepat (`Rekening`, `Target`, `Kategori`, `Cocokkan Saldo`) agar Alokasi Dana/Jadwal Rutin tidak diulang sebagai shortcut → ringkasan **Investasi** compact ketika tersedia → insight kondisi keuangan → satu Rencana Keuangan prioritas → satu Jadwal Terdekat → tiga Aktivitas Terbaru. Hero memakai `nonInvestmentBalance`, menghitung rekening aktif non-investasi, lalu menampilkan `Aman digunakan` dan `Batas aman per hari`; Saldo RDN tidak masuk saldo operasional, safe-to-spend, atau dana belum dialokasikan. Kartu ATM/rekening, panel detail Alokasi, dan panel arus kas tidak lagi diduplikasi di Beranda karena detailnya tetap tersedia di route canonical masing-masing. Alert lain tetap tersedia di Notification Center, transaksi baru memakai tombol `+` global, dan header hanya menampilkan status `Diperbarui` tanpa sinkron manual. Filter/search lengkap tetap di `/transaksi`; recent slice Dashboard tidak dipresentasikan seolah-olah mewakili seluruh ledger. Transfer baru pada viewport mobile memakai presentasi `mobile-transfer` dari `TransactionForm` canonical, bukan form terpisah Bila Rencana/Jadwal/Aktivitas belum berisi data, section tetap discoverable tetapi memakai empty-action dashed berikon `+` dengan helper satu baris; klik membuka form canonical secara otomatis jika konteks/capability cukup, tanpa auto-submit. Kebutuhan memilih/menentukan Alokasi Dana secara capability-aware, Jadwal membuka create rule, dan Aktivitas membuka Transaction Composer global.
- Ringkasan Alokasi Dana menjumlahkan nominal `used_amount + reserved_amount` sebagai **terpakai + dipesan** dan tidak lagi memilih alokasi pertama berdasarkan urutan nama. Perhitungan saldo/ledger tidak berubah.
- Mutation biasa yang berakhir `OUTCOME_UNKNOWN` mempertahankan metadata idempotency intent aman lintas reload (action, fingerprint hash, key, timestamp; tanpa payload finansial) di storage browser yang di-namespace per session/user. Payload berbeda untuk action yang sama diblok sampai intent lama mendapat hasil definitif; form transaksi mengunci field dan menyediakan retry data yang sama. Reset/full reset tetap memakai recovery/status workflow khusus.
- **Reset data testing** tetap Administrator-only dan fail-closed terhadap environment tidak dikenal: `reset.preview` dan `reset.apply` tersedia bila database terikat canonical sebagai `development` atau `production`, sehingga semua Administrator aktif dapat membersihkan data trial pada fase awal termasuk dari Vercel Production yang dibuka lewat `npm run prod`. `unbound`/marker asing ditolak sebelum safety backup, maintenance lock, atau purge; `reset.status` tetap dapat dibaca untuk reconciliation/recovery intent lama. Frontend memakai `system.health` sebagai authority environment, bukan mode build.
- Close/reopen periode serta perubahan anggota menginvalidasi projection yang bergantung, termasuk transaksi/dashboard, agar capability dan label tidak tertahan cache lama.
- Service Worker v10 tetap network-only untuk `/api/*`; stable image memakai stale-while-revalidate sehingga asset publik dapat diperbarui setelah deployment tanpa menunggu cache lama habis.

## Workspace Perencanaan saat ini

- Navigation menampilkan grup `Perencanaan` dengan `Perencanaan` dan `Target`. Workspace Perencanaan memakai dua tab canonical: `Alokasi Dana` (`/perencanaan/kantong`) dan `Jadwal Rutin` (`/perencanaan/jadwal`). `/anggaran` dan `/perencanaan/kebutuhan` dipertahankan hanya sebagai compatibility redirect ke `/perencanaan/kantong`; route `/alokasi` dan `/tagihan` juga tetap compatibility redirect.
- Alokasi Dana tidak meminta kategori. Klik item Alokasi Dana membuka detail dana, Kebutuhan kategori, dan Jadwal Rutin terkait. Kebutuhan tetap memakai record `budgets` dan relasi `budgets.envelope_rule_id` existing. Kategori yang sama dapat dipakai pada beberapa Alokasi Dana karena pencocokan Kebutuhan juga memasukkan `envelope_rule_id`; budget lama tanpa relasi dapat dihubungkan saat kategori tersebut pertama kali dipakai di detail Alokasi Dana. Pemakaian hanya menghitung transaksi kategori yang benar-benar memakai Alokasi Dana tersebut. Tidak ada migration/schema baru.
- Overview Alokasi memakai hierarchy ringkas: `Alokasi baru` berada di level halaman, kartu berisi ringkasan kondisi + preview Kebutuhan dan satu pintu `Buka alokasi`; kartu tanpa Kebutuhan boleh menawarkan `Tambah kebutuhan`. `Pindahkan dana`, pengingat, serta lifecycle dipusatkan di bagian **Pengaturan alokasi** pada detail. `Atur dana` juga berada di sana ketika kondisi normal, tetapi saat ada gap Kebutuhan banner gap mengambil alih sebagai satu-satunya entry point dan membawa nominal saran. True-empty tetap memakai satu primary CTA `Buat Alokasi Dana`, dan FAB mobile tetap khusus transaksi.
- Detail Alokasi Dana memakai satu master card terstruktur: hero + metrik, ringkasan Kebutuhan, grouped list Kebutuhan dengan ikon kategori/status/progress, dan status jadwal langsung pada setiap Kebutuhan. Panel `Jadwal Terkait` terpisah dihapus karena menduplikasi informasi. `Terpakai kebutuhan` dijumlahkan dari `budget.used_amount`; perbandingan pendanaan tetap terhadap `allocated_amount`. Aksi jarang dipusatkan pada bagian **Pengaturan alokasi**, tanpa tombol `Lainnya` di overview.
- Penutupan periode Alokasi Dana sekarang selalu menyiapkan periode aktif berikutnya agar rule aktif tidak menjadi state tanpa periode. Policy `unallocated` memulai periode berikutnya pada Rp0 dan mengembalikan sisa ke dana tersedia; policy `carry` hanya membawa sisa aktual dan tetap membuat periode berikutnya bila sisa Rp0. User dapat memilih `Pakai lagi kebutuhan di periode berikutnya`; pilihan ini hanya menyalin kategori dan nominal rencana Kebutuhan yang belum ada di periode tujuan, tidak menyalin transaksi, saldo, atau dana, dan tidak menimpa rencana yang sudah dibuat user.
- Jadwal Rutin tidak lagi mengekspos penanda Auto-debit. Occurrence yang jatuh tempo menunggu konfirmasi transaksi aktual; ledger/saldo tidak berubah sebelum transaksi aktual berhasil disimpan. Bila kategori Kebutuhan memiliki relasi Alokasi Dana yang tidak ambigu, Jadwal dapat menyarankan rekening sumber dan konfirmasi aktual menyarankan Alokasi Dana terkait tanpa mempercayai data client untuk authorization.
- Target, Alokasi Dana, Jadwal Rutin, dan Anggota tetap memakai artwork existing secara dekoratif. Nominal, status, capability, authorization, saldo, serta ledger tetap berasal dari read model/service canonical.
- Alokasi Dana tetap account-bound pada rekening **non-investasi**: item baru wajib satu rekening sumber operasional, read model rekening menyediakan saldo fisik, dana dialokasikan, dan dana tersedia; rekening `account_type=investment`/RDN ditolak untuk Alokasi baru dan tidak ikut `unallocatedFunds`. Transaksi beralokasi wajib memakai rekening yang sama, transaksi bebas/Transfer tidak boleh mengambil dana yang sudah dialokasikan, dan realokasi baru lintas rekening ditolak. Data Alokasi legacy yang pernah terikat RDN tetap readable tetapi diperlakukan non-operasional dan tidak boleh mengikat Saldo RDN baru.
- Capability planning kini dipakai konsisten pada read model Dashboard dan flow frontend: setup checklist serta pilihan Alokasi untuk transaksi/Jadwal Rutin mengikuti `can_*` backend; transfer composer memakai `transferRoutes` bootstrap server-side dan Target hanya mengekspos penarikan bila destination valid tersedia. Empty/create state mengarahkan prerequisite rekening alih-alih membuka modal yang tidak dapat diselesaikan.
- Dana tersedia kini dapat ditambahkan ke Alokasi Dana existing atau dilepas kembali melalui `envelopes.adjustAllocation` tanpa membuat ledger transaction. Member dapat mengelola planning shared serta Alokasi Dana, Kebutuhan, dan Jadwal Rutin pada rekening personal miliknya sendiri sesuai RFC-0016; Target baru tetap Bersama. Lifecycle destruktif/recovery tetap Administrator-only.
- Dashboard desktop tetap memisahkan dana tersedia yang belum dibagi dari pengeluaran tanpa Alokasi Dana dan menyediakan CTA berbeda; Beranda mobile merangkum kondisi tersebut melalui insight compact agar tidak menggandakan panel planning. First-run checklist hanya menganggap Rekening dan Kategori sebagai fondasi wajib untuk mulai mencatat transaksi. Alokasi Dana, Jadwal Rutin, dan Target tetap mudah dijangkau sebagai perencanaan opsional; Target memberi warning bila belum ada rekening sumber lain yang kompatibel untuk setoran.
- Transfer canonical memakai authority rekening sumber: Member hanya dapat mendebit shared atau personal miliknya, tetapi rekening personal pasangan boleh menjadi tujuan. Transfer personal lintas pemilik mengikuti ownership sumber; shared → personal oleh Member masuk approval request Administrator. Target bersama dapat menerima setoran dari rekening shared atau rekening personal actor yang kompatibel dengan aturan representability yang sama; Member tetap tidak memperoleh akses ke rekening personal pasangan. Transfer dan mutasi Target tetap netral terhadap income/expense.
- Dashboard, Transaksi, Laporan, Rekening, Kategori, dan Pengaturan sengaja tidak diberi artwork hero tambahan karena masing-masing sudah memiliki chart, kartu domain, icon taxonomy, atau utility hierarchy sebagai fokus utama.
- Empat ilustrasi reuse path login existing (`piggy-bank`, `wallet`, `finance-checklist`, `house`) dengan semantic dekoratif kosong agar login yang sudah stabil tidak memerlukan perpindahan asset.

## Transparansi keluarga dan compatibility cost-share

- Full transparency adalah policy produk canonical: seluruh rekening/ledger keluarga readable oleh kedua pengguna terotorisasi. Scope `personal` hanya menunjukkan pemegang/capability operasi; bukan privacy boundary. RFC-0015 berstatus Rejected.
- Schema v11 `transactions.cost_share_mode`/`cost_share_json` tetap dipertahankan untuk compatibility histori, audit, backup/restore, integrity, export, dan API lama. UI canonical transaksi/pembayaran rutin baru memakai `unspecified` dan Laporan tidak lagi menampilkan panel split.
- `created_by` tetap aktivitas pencatatan dan kini menjadi konteks eksplisit pada Aktivitas Terbaru Beranda. Relasi refund ke expense asli tetap deferred sesuai RFC-0013; payer/beneficiary/settlement internal tidak diprioritaskan selama model produk tetap satu keuangan keluarga.

## Laporan mobile saat ini

- `/laporan` pada viewport ≤820px memakai hierarchy analitik compact yang terpisah dari workspace desktop, tetapi tetap membaca action canonical `reports.monthly`.
- Mode `Ringkasan` menampilkan tren pengeluaran 3/6/12 bulan, arus kas bersih, **Saldo rekening**, **Aman digunakan**, snapshot **Total kekayaan tercatat · saat ini** (`nonInvestmentBalance + portfolio_value` tanpa double-count Saldo RDN), perbandingan dengan bulan sebelumnya, kategori pengeluaran terbesar, serta seluruh alert actionable. Tren **Saldo utama** memakai `nonInvestmentBalance`; `totalBalance` yang masih merekonsiliasi Saldo RDN dan `investment_account_events` dipertahankan hanya sebagai field compatibility/read-model dan tidak dipakai sebagai Saldo utama.
- Mode `Per kategori` menampilkan distribusi kategori memakai ikon kategori canonical, analisis Kebutuhan vs aktual read-only, dan progressive disclosure untuk breakdown rekening, nature, serta aktivitas pencatatan.
- Navigasi periode mendukung bulan sebelumnya/berikutnya sampai bulan berjalan dan picker bulan native. Perbandingan dihitung dari `trend.items` yang sama, sehingga tidak menambah request, schema, mutation, atau business rule baru.
- Desktop `/laporan` tetap memakai panel analitik existing. Backend, auth, saldo, ledger, authorization, dan contract API tidak berubah oleh redesign mobile ini.
- Route `/laporan` kembali buildable setelah import ikon `FiWallet` yang tidak tersedia pada `react-icons/fi` diganti dengan export Feather yang valid. Regression import-symbol dan production build menjadi guard agar route lazy tidak kembali gagal dibuka karena named export invalid.

## Rekening mobile saat ini

- `MobileAccountsExperience` tetap lazy untuk menjaga route-chunk budget. Capability mobile/desktop divalidasi oleh frontend regression, rendered anonymous browser smoke di quality gate, dan pemeriksaan manual pada authenticated/device viewport relevan.
- Transfer adalah quick action, bukan tab. Form tetap memakai `TransactionForm` canonical dan sukses hanya ditampilkan setelah server mengonfirmasi write.
- Rekening mobile memakai carousel horizontal circular yang app-like: kartu aktif di tengah, kartu tetangga mengintip dari sisi, swipe kiri/kanan + Arrow Left/Right + dots mengubah selection, dan vertical document scroll tetap bebas. Artwork transparan tampil langsung tanpa wrapper putih/hitam; saldo tidak lagi ditumpuk di atas motif kartu. Kartu samping sekarang lebih redup/kecil, active dot berbentuk pill, summary dibuat lebih compact, dan grafik pengeluaran utama dihapus dari route Rekening karena analitik sudah dimiliki Riwayat/Transaksi mobile.
- Di bawah kartu aktif, rekening non-investasi memakai summary **Dana tersedia** + dua submetric **Saldo / Dialokasikan** tanpa mengulang nominal tersedia. Rekening Investasi memakai presentasi khusus **Saldo RDN** + konteks `Tujuan dana Investasi` tanpa mengulang Saldo RDN sebagai submetric. Toggle visibilitas nominal lokal tetap konsisten, lalu quick action `Riwayat / Transfer`; `Transaksi terbaru` tampil langsung. Grafik pengeluaran dipindahkan ke disclosure sehingga Transfer tetap quick action dan tetap tidak dihitung sebagai pemasukan/pengeluaran.
- Form Tambah rekening mobile memakai selector jenis 2 kolom dengan label utuh. Tunai, Dana darurat, dan E-wallet memakai nama canonical/default agar user tidak dipaksa mengisi nama yang tidak diperlukan; backend `name` dan duplicate guard tetap dipertahankan. Kepemilikan memakai label ringkas `Bersama`, `Saya`, atau nama depan pasangan.
- Kartu rekening memakai asset WebP 1024×645 untuk bank, Tunai, Tabungan, serta provider E-wallet ShopeePay, DANA, GoPay, OVO, dan LinkAja; ukuran raster dijaga maksimal 100 KB per asset dan komponen mendeklarasikan dimensi intrinsik agar layout stabil. Lima artwork E-wallet canonical sudah menggunakan visual baru yang mengisi kanvas secara natural, rounded silhouette bersih, dan alpha transparan di luar kartu tanpa strip/matte gelap, sehingga aman pada light/dark surface. Provider E-wallet disimpan canonical pada `accounts.ewallet_template` schema v8; nama rekening hanya dipakai sebagai fallback untuk object/backup legacy yang belum memiliki field tersebut. Provider `generic` tetap aman untuk E-wallet lain.
- Surface kartu berbasis image sekarang transparan di seluruh consumer `AccountVisual`; bank artwork memakai bleed canonical sehingga alpha transparan tidak lagi memperlihatkan matte gelap pada Dashboard desktop, detail Rekening, atau carousel route Rekening. Beranda mobile tidak memakai `AccountVisual`. Pada carousel mobile `/rekening`, shadow diterapkan ke artwork, bukan kotak wrapper, sehingga alpha sudut tetap menyatu natural dengan background.


## Notifikasi dan pengingat saat ini

- Tujuh pengingat otomatis existing tetap dijadwalkan server dan memakai dedupe canonical.
- Pengingat manual one-shot tersedia langsung pada Kebutuhan periode aktif, Alokasi Dana aktif, occurrence Jadwal Rutin yang belum selesai, dan Target aktif. Pengingat hanya milik actor, disimpan server-side, memakai `row_version`, idempotency, audit, dan diproses scheduler.
- Push produk memakai privacy-safe lock-screen: copy detail boleh tetap ada di queue server, tetapi transport Web Push hanya membawa tipe/id/target dan Service Worker menampilkan copy generik tanpa nominal, rekening, merchant, atau nama objek finansial. Logo aplikasi tetap dipakai sebagai `icon` dan badge monokrom khusus Android.
- Reminder manual menampilkan `lastDispatch` dari queue, menolak jadwal baru selama dispatch lama masih nonterminal, dan dibatalkan atomik saat entity selesai/ditutup/diarsipkan/dihapus; integrity check memeriksa drift user/entity/queue. UI memberi warning bila Web Push perangkat belum siap tanpa menganggap penyimpanan reminder gagal.
- Real-device Android/iOS/desktop tetap memerlukan QA operasional karena browser/OS menentukan bentuk final notification card dan lock-screen privacy.

- Tooling environment memisahkan dua profile yang wajib ada pada setiap workstation tepercaya: `.env.local` Development-only dan `.env.production.local` Production-only. `npm run dev` hanya auto-refresh Development dan tidak menyentuh `.env.production.local`; `npm run prod` tidak pull/menimpa `.env.local`, memvalidasi isolasi profile, boleh menyejajarkan hanya grup Google bridge pusat ke profile Production lokal yang kosong, memeriksa Turso Production read-only, lalu Vercel Production aktual agar keberhasilan Development tidak dianggap bukti Production. Core public health kini hanya memblokir database/schema/binding, maintenance, atau integrity failure. Scheduler, Google integration, backup, dan notification degradation tetap dilaporkan sebagai operational warning; `npm run prod` dapat membuka login/ledger bila core Production + frontend sehat sambil tetap menampilkan diagnosis tanpa membocorkan secret.
- Tooling environment juga menyediakan `npm run env:pull:development` dan `npm run env:status` untuk bootstrap/troubleshooting lintas-PC tanpa menyalin secret manual. Checker Production menjaga public Firebase/Google config tetap selaras dan menolak database/token/session/VAPID yang dibagi lintas Dev/Prod; fingerprint Web Push yang ditampilkan bersifat public-only.

## Open operational risks

1. GitHub `main` harus memblok force push/delete dan menjalankan workflow `Quality` pada push. Direct push normal dipertahankan untuk workflow private sederhana; jangan gunakan `--no-verify`.
2. Source sudah memblokir cross-environment database. Operasional tetap perlu menjaga evidence bahwa Development/Production menunjuk resource berbeda, tetapi ADR-0007 sudah berstatus Superseded dan tidak lagi menjadi blocker runtime.
3. Production schema/runtime parity dan resource Google nyata harus diverifikasi melalui runbook.
4. Real-device Web Push dan restore drill memerlukan evidence operasional bila belum dilakukan.
5. Secret rotation/revocation mengikuti runbook; source tidak dapat membuktikan credential lama sudah dicabut.
6. External operational alerting belum tersedia; health/backup/dead-letter masih memerlukan monitoring operator.
7. Session v2/device revoke sudah tersedia di source; real-device smoke, forced legacy re-login, dan operational revoke drill tetap memerlukan evidence deployment.
8. Retention `notification_queue`, `notification_deliveries`, `integration_outbox`, `backup_runs`, dan `integrity_runs` belum memiliki jadwal purge otomatis; audit/ledger tetap tidak boleh dipurge oleh housekeeping biasa.
